@model IEnumerable<KeToanGiaThanhSanPham.Models.PhanXuong>

@{
    ViewData["Title"] = "Index";
}

<h1>DANH MỤC PHÂN XƯỞNG</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.MaPhanXuong)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TenPhanXuong)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(model => item.MaPhanXuong)
            </td>
            <td>
                @Html.DisplayFor(model => item.TenPhanXuong)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
